<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/food_donation">

    <fragment
        android:id="@+id/food_donation"
        android:name="com.example.bottomnavyt.food_donation"
        android:label="fragment_food_donation"
        tools:layout="@layout/fragment_food_donation" >
        <action
            android:id="@+id/action_food_donation_to_food_donation_detail"
            app:destination="@id/food_donation_detail" />
    </fragment>
    <fragment
        android:id="@+id/food_donation_detail"
        android:name="com.example.bottomnavyt.food_donation_detail"
        android:label="fragment_food_donation_detail"
        tools:layout="@layout/fragment_food_donation_detail" />
</navigation>